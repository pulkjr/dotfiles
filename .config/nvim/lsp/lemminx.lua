return {
    settings = {
        xml = {
            format = {
                maxLineLength = 0, -- disables line wrapping
                splitAttributes = true, -- optional: keeps attributes on one line
                insertSpaces = true, -- true = use spaces; false = use tabs
                tabSize = 2, -- number of spaces per indent level
            },
        },
    },
}
