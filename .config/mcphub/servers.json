{
  "mcpServers": {
    "filesystem": {
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount",
        "type=bind,src=${CWD},dst=/projects",
        "mcp/filesystem",
        "/projects"
      ],
      "command": "docker",
      "disabled": false
    }
  },
  "nativeMCPServers": {}
}
