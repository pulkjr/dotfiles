# NOTEBOOK SETTINGS
[notebook]
dir = "~/git/Personal/dailyZK"

[note]
filename = "{{title}}"

# EXTRA VARIABLES
[extra]
author = "pulk"

# EXTERNAL TOOLS
[tool]

# Default editor used to open notes.
editor = "nvim"

# Default shell used by aliases and commands.
shell = "/bin/zsh"

# Command used to preview a note during interactive fzf mode.
fzf-preview = "bat -p --color always {-1}"

# NAMED FILTERS
[filter]
recents = "--sort created- --created-after 'last two weeks'"


# COMMAND ALIASES
[alias]

# Edit the last modified note.
last = "zk edit --limit 1 --sort modified- $@"

# Edit the notes selected interactively among the notes created the last two weeks.
recent = "zk edit --sort created- --created-after 'last two weeks' --interactive"

# Open the zk conf file
conf = '$EDITOR ~/.config/zk/config.toml'
config = '$EDITOR ~/.config/zk/config.toml'

# Create a new note
n = 'zk new --title "$@"'

# Create a new azure note
az = 'zk new --title "$@" --template azure.md "$ZK_NOTEBOOK_DIR/azure"'

# Show a list of notes tagged as servers
azure = "zk edit --sort created- --tag 'azure' --interactive"

map = 'zk new --title "$@" --template map.md "$ZK_NOTEBOOK_DIR/maps"'

[group.azure]
# Directories listed here will automatically use this group when creating notes.
paths = ["azure"]
template = "azure.md"
extension = "md"
filename = "{{date:YYYYMMDDHHmm}}-{{slug title}}"

[group.map]
paths = ["maps"]
template = "map.md"
extension = "md"

